# Tuples are inmutable sequences (so can't be changed in place)
# t = "a", "b", "c"
t = ("a", "b", "c") # optional use of paranethesis
print(t) # tuples are in parenthesis rather than brackets

name = "Jon"
age = 26
print(name, age, "Python", 2020)
print((name, age, "Python", 2020)) # parenthis must be used when tuple is argument
